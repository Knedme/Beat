# Config file

token = "bot-token"  # bot token

prefix = "+"  # command prefix

ffmpeg = "C:/ffmpeg/ffmpeg.exe"  # ffmpeg path

cookies = "D:/BeatCookies/cookies.txt"  # cookies file path (For age registered videos)

ffmpeg_options = {  # ffmpeg options
	"before_options": "-reconnect 1 -reconnect_streamed 1 -reconnect_delay_max 5",
	"options": "-vn"
}
